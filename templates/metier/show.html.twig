{% extends 'base.html.twig' %}

{% block title %}Metier{% endblock %}

{% block body %}
    <h1>Metier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metier.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ metier.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn-link" href="{{ path('app_metier_index') }}">Retour à l'index</a>
    <a class="btn-link" href="{{ path('app_metier_edit', {'id': metier.id}) }}">Edition</a>

{% endblock %}
