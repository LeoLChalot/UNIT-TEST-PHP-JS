{% extends 'base.html.twig' %}

{% block title %}Créer un nouvel utilisateur
{% endblock %}

{% block body %}

	<div class="ROW">
		<h1 class="TitlePage">Créer un nouvel utilisateur</h1>
		<a href="{{ path('app_user_index') }}" class="BtnEmploye  a-btn">Retour à la liste</a>
	</div>

	{{ form_start(form) }}

	<div class="FormNewEmploye gridCol2">
		<div>
			{{ form_row(form.nom) }}
			{{ form_row(form.prenom) }}
			{{ form_row(form.telephone) }}
		</div>
		<div>
			{{ form_row(form.email) }}
			{{ form_row(form.plainPassword, {'label': 'Mot de passe :'}) }}
			{{ form_row(form.roles, {'label': 'Rôles :'}) }}


		</div>
		<button class="form-btn-add">Créer l'utilisateur</button>
	</div>


	{{ form_end(form) }}
{% endblock %}
